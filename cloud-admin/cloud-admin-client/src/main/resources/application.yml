server:
  port: 8080

spring:
  application:
    name: cloud-admin-client
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
management:
  security:
    # 关闭安全验证
    enabled: false